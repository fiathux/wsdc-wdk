from WDK.WebGI.container.wdkenv import basicEnvdef getMWEnv(proc=None,custom=None,**arg):    if custom: cEnv=custom    else:        cEnv = basicEnv(**arg)    if proc: cEnv=proc(cEnv)    def mw_env(appmw):        return lambda env,rpo: appmw(cEnv.begin(env),rpo)    return mw_env